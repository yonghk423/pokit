/**
 * 하루 일과 온보딩 전용 일러스트 — 루트 `assets/` 만 사용 (`src/` 에셋 제외).
 */
export const dailyRhythmOnboardingAssets = {
  /** 히어로 — 아침 러닝 */
  hero: require('../../../../assets/main.png'),
  /** 하루 시작 행 — 아침 루틴 */
  startThumb: require('../../../../assets/splash3.png'),
  /** 하루 마무리 행 — 저녁 독서 */
  endThumb: require('../../../../assets/pokit4.png'),
} as const;
