---
description: App Store / 앱 소개 스크린샷(6.9인치) 합성 패턴. 사용자가 스크린샷·소개 이미지·앱스토어 이미지를 요청할 때 적용.
alwaysApply: false
---

# 앱스토어 스크린샷 (6.9")

## 캔버스

- **필수 크기:** `1320 × 2868` px (App Store Connect 6.9" Display)
- **배경:** 민트 세로 그라데이션 `#D9F8FB` → `#C4ECE8` (또는 RGB 217,248,251 → 196,236,232)
- **점선 카드/외곽 프레임은 쓰지 않음** — 민트 풀블리드만
- 출력: `assets/store/iphone-6.9-<slug>.png` + 바탕화면 `POKIT-6.9-<slug>.png`

## 헤드라인

- 좌상단, Apple SD Gothic Neo **Bold**, 잉크 `#141618`
- 2줄로 자연스럽게 끊기. 줄 간격은 **아주 살짝** (gap ≈ 18px @1x / LINE_GAP 36 @2x)
- 사용자가 준 멘트를 **그대로** 씀 (임의 수정·「루틴」치환 금지 — 마케팅 카피)

## 폰 목업 (검증된 2D만)

- **실기기/시뮬레이터 캡처**를 넣는다. AI로 UI를 다시 그리지 않는다.
- 원본 우선: Desktop `Simulator Screenshot - iPhone 17 Pro Max - …png` (**1320×2868**). 채팅 첨부가 471×1024면 업스케일하되, 가능하면 고해상도 원본을 먼저 찾는다.
- **화면 비율을 자르지 않는다.** 항상 `1320:2868` 유지한 채 스케일만.
- 베젤: 밝은 티타늄/화이트 + 얇은 다크 립, 라운드 ≈ 화면폭 비율의 165px
- 그림자: soft drop shadow
- 기울기: **시계방향 약 −7.4°** (단일·듀얼 모두 동일 방향)
- **CSS 3D / 옆면 두꺼운 입체 목업은 쓰지 않는다** (반려됨)

## 레이아웃 A — 단일 폰

예: 오늘, 하루 밸런스, 책방, 스토리

1. 헤드라인 아래
2. 화면 폭 ≈ **1088px @1x**, 가로 중앙
3. 하단은 캔버스 밖으로 잘려도 됨

## 레이아웃 B — 듀얼 폰 (세로 스택)

예: 루틴+히스토리, 빠른메모+노트

1. **같은 기울기(−7.4°)**, 위·아래 배치 (좌우 대칭 기울기 금지)
2. 화면 폭 ≈ **700px @1x**
3. 위 폰: 중앙에서 **왼쪽·아래**로 약간 오프셋 (`≈ −100x, +130y` @1x 기준)
4. 아래 폰: 위 폰과 **살짝 겹침** (`overlap ≈ 420px @1x`), 아래 레이어가 위
5. 하단 잘림 OK

## 합성 방법

- Python Pillow로 2× 렌더 후 `1320×2868`로 다운스케일 (텍스트 선명도)
- 폰트: `/System/Library/Fonts/AppleSDGothicNeo.ttc` index 6 (Bold)

## 금지

- 폰 화면만 납작하게 크롭해 비율 깨기
- 3D 원근(옆면 과다)·서로 반대 방향 기울기
- 헤드라인·UI 카피를 멋대로 바꾸기
